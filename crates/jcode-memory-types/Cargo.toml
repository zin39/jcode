[package]
name = "jcode-memory-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
rand = "0.9.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
