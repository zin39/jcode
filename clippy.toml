# Clippy configuration for the jcode workspace.
# Lint levels live in [workspace.lints.clippy] in the root Cargo.toml;
# this file only tunes lint behavior.

# dbg! is denied in production code but fine inside #[cfg(test)].
allow-dbg-in-tests = true
allow-print-in-tests = true
